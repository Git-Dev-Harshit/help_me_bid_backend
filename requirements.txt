# ---------------------------------------------------------------------------
# Runtime dependencies - the SINGLE source of truth, pinned for reproducible
# Docker builds. The Docker build installs from this file, and pyproject.toml
# reads it via `dynamic = ["dependencies"]`, so there is no second list to keep
# in sync.
#
# Dev/test tooling lives in requirements-dev.txt.
# ---------------------------------------------------------------------------

# --- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# --- Settings / validation ----------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ------------------------------------------------------------
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# --- Security / auth -----------------------------------------------------
PyJWT==2.10.1
argon2-cffi==23.1.0
phonenumbers==8.13.52

# --- Scraping ------------------------------------------------------------
httpx[http2]==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Scheduling ----------------------------------------------------------
APScheduler==3.11.0
