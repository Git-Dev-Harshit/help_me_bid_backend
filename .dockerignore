.git
.gitignore
.venv
venv
__pycache__
*.py[cod]
*.egg-info
.mypy_cache
.ruff_cache
.pytest_cache
.coverage
htmlcov
.env
.env.*
!.env.example
docs
*.md
!README.md
.vscode
.idea
.DS_Store
Thumbs.db

# NOTE: tests/ is deliberately NOT ignored - the Dockerfile's `test` stage
# copies it. It never reaches the `runtime` stage, so the production image
# still ships without it.
